name: dotnet package

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      shell: cmd
      run: build.bat
    - name: Test
      shell: cmd
      run: build\DBMAbout.exe
